id: bb8421b0-1685-4e3e-993d-f4fbe64a5ec3
name: amex
type: product
description: amex interview
mission: Amex interview
created_at: 2026-09-10T09:17:35.157366-06:00
concepts:
    - name: projects
      path: projects/
      description: Active development projects
      depth: 1
      ignore:
        - worktrees/
    - name: workflow
      path: workflow/
      description: Workflows
      children:
        - name: festivals
          path: festivals/
          description: Multi-step festival plans
        - name: design
          path: workflow/design/
          description: Design documents
          depth: 1
        - name: explore
          path: workflow/explore/
          description: Exploratory notes and discovery work
          depth: 1
        - name: reviews
          path: workflow/reviews/
          description: Reviews
          depth: 1
    - name: docs
      path: docs/
      description: Documentation
      depth: 0
intents:
    tags:
        - personal
        - reference
        - question
        - follow-up
workflows:
    categories:
        pipeline:
            label: Pipeline
            description: Structured camp data movement, compilation, ingestion, or export
        plan:
            label: Plan
            description: Planning, design, intents, festivals, and structured execution work
        research:
            label: Research
            description: Exploration, comparison, discovery, and investigation
        review:
            label: Review
            description: Review-style work such as code reviews and quality passes
    category_by_type:
        code_reviews: review
        design: plan
        explore: research
        festival: plan
        intent: plan
        pipelines: pipeline
        reviews: review

# hooks:
#   commit_message:
#     # Command run from the target repo working tree by 'camp commit --auto-write'
#     # (-aw). Its stdout is used verbatim as the commit message. Any tool works;
#     # 'ob commit --print-session-id' is one example; progress and any
#     # session_id= line on stderr are forwarded to the terminal (session_id is
#     # emitted when the hook finishes, not while it is generating). Amend
#     # auto-write invocations receive CAMP_COMMIT_AMEND=1.
#     #
#     # Run as written, so any bound of the writer's own belongs here rather
#     # than in camp. A deferred run has nobody watching it and can afford to
#     # be more patient than one you are waiting on, which is worth spending on
#     # a local model.
#     command: ob commit --print-session-id
#     # How long a deferred run of that command may take before camp stops it
#     # and parks the job in failed/. Foreground runs are not bounded: you are
#     # watching those and can interrupt them yourself. Default 12m.
#     timeout: 12m
