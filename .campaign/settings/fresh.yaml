# fresh.yaml — Post-merge branch cycling configuration for `camp fresh`.
#
# After merging a PR, `camp fresh` syncs your project to its default branch,
# prunes merged branches, and optionally creates a new working branch.
#
# Usage:
#   camp fresh                    # Sync current project using this config
#   camp fresh --branch feat/x   # Override branch for this run
#   camp fresh all                # Sync all project submodules
#   camp fresh --dry-run          # Preview without changes

# Branch to create after syncing. Leave empty to stay on the default branch.
# branch: develop

# Push new branches upstream with --set-upstream (default: true).
# push_upstream: true

# Prune local branches already merged into the default branch (default: true).
# prune: true

# Prune stale remote tracking refs via `git remote prune origin` (default: true).
# prune_remote: true

# Per-project overrides. Keys are project names (directory name under projects/).
# projects:
#   camp:
#     branch: feat/camp
#     push_upstream: true
#   fest:
#     branch: feat/fest
#     push_upstream: false

# Follow-up command workflows run, in order, after a successful sync/prune/
# branch cycle. Manage these with `camp fresh configure show|add|remove`
# instead of hand-editing this list, though hand-editing works too.
#
# Each step: name (required, unique within its scope), run (required shell
# command), dir (optional, relative to the project root), and
# continue_on_error (optional, default false; set true to keep running
# later steps even if this one fails).
#
# follow_up:
#   - name: install
#     run: npm install

# A project's follow_up list under `projects` replaces the global list above
# entirely for that project; it is never merged with it.
# projects:
#   camp:
#     follow_up:
#       - name: build
#         run: go build ./...
#         dir: cmd/camp
#         continue_on_error: false

# Skip follow-ups for a single run with `camp fresh --no-follow-up`. They are
# never executed on `camp fresh --dry-run`, only listed in the preview.
